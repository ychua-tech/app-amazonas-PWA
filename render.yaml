services:
  - name: amazonas-api
    type: web
    runtime: node
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      # preenchido na mão no dashboard do Render na 1ª vez (nunca fica neste arquivo)
      - key: ADMIN_TOKEN
        value:
        sync: false
