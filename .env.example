# URL da API do supermercado (servidor em ./server).
# Use o IP da máquina na rede local para o celular enxergar — não "localhost".
# Sem esta variável, o app roda com dados de exemplo (modo demo).
EXPO_PUBLIC_API_URL=http://192.168.0.10:3333
